#include<iostream>
using namespace std;
int main()
{
    int marks[n],i,sum=0;
    for(i=0;i<n;i++)
    cout<<"Enter the size of an array" <<endl;
    {
        cout<<(i+1)<<". Enter marks: "<<endl;
        cin>>marks[i];
        sum=sum+marks[i];
    }
    cout<<"sum is "<<sum<<endl;
}